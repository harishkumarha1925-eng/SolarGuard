import streamlit as st
st.set_page_config(page_title="SolarGuard - Smoke Test", layout="centered")
st.title("SolarGuard — Deployment Smoke Test (NO TORCH)")
st.write("If you see this, the new entry file is deployed and *torch is not imported at startup*.")
